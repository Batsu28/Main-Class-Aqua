// 1

/*
let mystring = "sfahfJ";

console.log(mystring.length);
console.log(mystring.charAt(0));
console.log(mystring[mystring.length - 1]);
console.log(mystring.toLocaleUpperCase());
console.log(mystring.toLowerCase());

let firstname = "djsh",
  fullname = firstname.concat("", "hask");

console.log(fullname);
*/

//2

/*
let babyshark = "baby shark doo-doo doo-doo";

let firstpart = babyshark.substring(0, babyshark.indexOf("ark"));
let lastpart = babyshark.substring(
  babyshark.indexOf("ark") + "ark".length,
  babyshark.length
);

firstpart = babyshark.substring(0, babyshark.indexOf("ark"));
lastpart = babyshark.substring(
  babyshark.indexOf("ark") + "ark".length,
  babyshark.length
);
babyshark = firstpart.concat(lastpart);

console.log(babyshark);
*/
console.log(Math.ceil(Math.random() * 6));
